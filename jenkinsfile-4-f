pipeline {
    agent any

    stages {
        stage('git code') {
            steps {
                git branch: 'main', url: 'https://github.com/bayriradha/lms-public.git'
            }
        }
    }
}

