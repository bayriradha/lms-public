pipeline {
    agent any

    stages {
        stage('Docker image') {
            steps {
                sh 'cd webapp && docker build -f Dockerfile -t di4front .'
		sh 'docker container run -dt --name cont1 di4front'
            }
        }

    }
}

